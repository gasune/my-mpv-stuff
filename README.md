# gasman's mpv config

My private MPV config files which are optimized to run on GTX 1060 6GB with a fairly decent CPU.

## Self Notes:
- Use Cera Pro/Cooper Medium BT for subs
- Use Vulkan API for slightly better performance, if buggy; return to opengl
- Use vo=gpu-next for testing
